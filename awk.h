AAWK
BAWK
	CAWK
DAWK
EAWK
	FAWK
		GAWK
	HAWK
IAWK
		JAWK
	KAWK
		LAWK
		MAWK
		NAWK
		OAWK
PAWK
	QAWK
	RAWK
	SAWK
		TAWK
UAWK
VAWK
	WAWK
	XAWK
YAWK
	ZAWK


struct asl_obj {
	unsigned int type;
	union data {
		als_str	str;
		als_int	um;
		void	*ref;
		als_flt	fnum;
	};
};

#define BUILTIN_TYPE_STR
#define BUILTIN_TYPE_INT
#define BUILTIN_TYPE_FLT
#define BUILTIN_TYPE_REF
#ifdef REGEX
#define BUILTIN_TYPE_REGEX
#endif
#define BUILTIN_TYPE_FILE
#define BUILTIN_TYPE_SOCK

enum token {
	TOK_OP_ASSN,
	TOK_OP_ADD,
	TOK_OP_SUB,
	TOK_OP_MUL,
	TOK_OP_DIV,
	TOK_OP_MOD,
	TOK_OP_POW,
	TOK_OP_ADD_ASSN,
	TOK_OP_SUB_ASSN,
	TOK_OP_MUL_ASSN,
	TOK_OP_DIV_ASSN,
	TOK_OP_MOD_ASSN,
	TOK_OP_POW_ASSN,
	TOK_OP_INC,
	TOK_OP_DEC,
	TOK_OP_CAT,
	TOK_OP_EQ,
	TOK_OP_NE,
	TOK_OP_GT,
	TOK_OP_GE,
	TOK_OP_LS,
	TOK_OP_LE,
	TOK_OP_MATCH,
	TOK_OP_NOMATCH,
	TOK_OP_OR,
	TOK_OP_AND,
	TOK_OP_REDIR_READ,
	TOK_OP_REDIR_WRITE,
	TOK_OP_REDIR_ADD,
	TOK_OP_PIPE,
	TOK_OP_LPARENTH,
	TOK_OP_RPARENTH,
	TOK_OP_LBRACKET,
	TOK_OP_RBRACKET,
	TOK_OP_LBRACE,
	TOK_OP_RBRACE,
	TOK_OP_COMMA,
	TOK_OP_DOLLAR,
	TOK_KW_IN,
	TOK_KW_DELETE,
	TOK_KW_BEGIN,
	TOK_KW_END,
	TOK_KW_FUNCTION,
	TOK_KW_IF,
	TOK_KW_ELSE,
	TOK_KW_WHILE,
	TOK_KW_FOR,
	TOK_KW_DO,
	TOK_KW_BREAK,
	TOK_KW_CONTINUE,
	TOK_KW_NEXT,
	TOK_KW_EXIT,
	TOK_KW_RETURN,
	TOK_KW_PRINT,
	TOK_KW_PRINTF,
	TOK_KW_GETLINE,
	TOK_KW_SWITCH,
	TOK_KW_CASE,
	TOK_KW_DEFAULT,
	TOK_KW_NEXTFILE,
	TOK_KW_BEGINFILE,
	TOK_KW_ENDFILE,
	TOK_KW_INIT,
	TOK_KW_FINAL,
	TOK_BF_LENGTH,
	TOK_BF_INT,
	TOK_BF_CLOSE ,
	TOK_BF_FFLUSH,
	TOK_BF_SYSTEM,
	TOK_BF_MATCH,
	TOK_BF_SUB,
	TOK_BF_GSUB,
	TOK_BF_SUBSTR,
	TOK_BF_INDEX,
	TOK_BF_SPRINTF,
	TOK_BF_SPRIT,
	TOK_BF_TOLOWER,
	TOK_BF_TOUPPER,
	TOK_BF_SQRT,
	TOK_BF_LOG,
	TOK_BF_EXP,
	TOK_BF_SIN,
	TOK_BF_COS,
	TOK_BF_ATAN2,
	TOK_BF_SRAND,
	TOK_BF_RAND,
	TOK_LT_NUMBER,
	TOK_LT_STRING,
	TOK_LT_REGEX,
	TOK_SP_EOS,
	TOK_SP_EOF,
};

enum {
	TYPE_STRING,
	TYPE_NUMBER,
};


NL ',' '{' '(' '|' ';' '/' ')' '}' '[' ']'
NOTMATCH
MATCHOP
EMPTYRE
AND
BOR
APPEND
EQ
GE
GT
LE
LT
NE
IN
ARG
BLTIN
BREAK
CLOSE
CONTINUE
DELETE
DO
EXIT
FOR
FUNC
IF
NEXT
NEXTFILE

SUB
GSUB
INDEX
LSUBSTR
MATCHFCN

ASSIGN
ASGNOP
ADDEQ
SUBEQ
MULTEQ
DIVEQ
MODEQ
POWEQ
PRINT
PRINTF
SPRINTF
ELSE
INTEST
CONDEXPR
POSTINCR
PREINCR
POSTDECR
PREDECR
VAR
IVAR
VARNF
CALL
NUMBER
STRING
REGEXPR





#if 0

正規表現
ネットワーク
POSIX
	signal;
	select;
	sleep;
	timer;
	file;






#endif
